# Copy to .env and fill in values (never commit .env)
FLASK_ENV=development
FLASK_DEBUG=1
ANTHROPIC_API_KEY=

# Optional when you add PostgreSQL
# DATABASE_URL=postgresql://user:pass@localhost:5432/skillgap
