# Copy to showcase/.env (never commit .env — it is gitignored).
# Never put a real API key in THIS file — .env.example is tracked in git.
GEMINI_API_KEY=
# Optional if the default model is unavailable in your region:
# GEMINI_MODEL=gemini-1.5-flash
