# Copy to showcase/.env (never commit .env — it is gitignored).
# Never put real secrets in THIS file — it is tracked in git.

# Flask API (PostingPal backend). Same machine: default below.
API_BASE_URL=http://127.0.0.1:5000
